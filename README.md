# dotfiles
dotfiles, but competent this time.

## Agent tooling

This repo supports Claude Code and Codex during migration. Shared agent content lives in `.agents/`; setup scripts link it into `~/.claude` and `~/.codex`.

Run `./setup.sh` on macOS or `./setup-linux.sh` on Linux to install and link the supported tools.
