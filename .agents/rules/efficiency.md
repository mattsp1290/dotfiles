# Efficiency

## Use what the user gives you

When the user provides a file path, function name, or specific location — use it directly. Do not start a broad codebase search or exploration when you already have the answer. Only explore if the provided path doesn't exist or the context is clearly insufficient.
